# ---- install.js 產生的建置產物 ----
# bin/install.js 會注入指令檔：-g 且從本機 repo checkout 執行時寫回 repo 根目錄，
# 其餘情況（npx 執行、workspace 模式）寫到 process.cwd()。
# 前者會把這些檔案寫進 repo 自己身上。它們是輸出、不是原始碼，一律不追蹤。
.cursorrules
.windsurfrules
.github/copilot-instructions.md

# 安裝器在 workspace 模式（未帶 -g）下產生的 skill 副本
.agents/
.claude/

# ---- 本機交接筆記 ----
# 跨 session 的工作交接用，內容只對當下這台機器有意義，不進版控。
HANDOFF.md

# ---- Node ----
node_modules/
npm-debug.log*
package-lock.json
